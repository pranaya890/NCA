manual page or `man` page is used to learn the manual of a command
`man ls` it will show the manual of `ls` command
man is usually located in `/usr/bin/man` 
we can use `man man` command to see manual of `man` command
we can search the list of manuals by using `man -k <keyword>`
![[Pasted image 20250723215613.png]]
this searches the manual with channel keyword
`-k` usually search the short manual page for keyword and display for matches
