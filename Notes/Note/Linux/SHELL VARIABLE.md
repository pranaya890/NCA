assigning the value to shell
```Shell
a=Hell0 
b="GROUND ZERO"
echo a
```

we can use `export variable_name` to export the value of variable in shell


we can use `env` command in shell to print out every exported variable
![[Pasted image 20250724231433.png]]

we can store the output into a variable
``` Shell
c= $(/flag) #captures theoutput of /flag in c

```
![[Pasted image 20250724231811.png]]
