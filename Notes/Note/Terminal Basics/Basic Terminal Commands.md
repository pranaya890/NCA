```shell
google-chrome #to open google chrome
firefox #to open firefox
```
We can basically use `<appname>` for opening the application using terminal

```Shell
sudo reboot #for rebooting the linux system
```

### SUDO
`sudo` is superuser do. 
has  privileges of superuser(root)
Temporary way to become a superuser to execute a command which require higher permission

### To break a process
`Ctrl +C`
### To clear the terminal
`clear`
